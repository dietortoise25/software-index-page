# 添加到 /etc/nginx/sites-enabled/software-index 中
# 位置：在 location /api/ 之前

    location /api/agent/ {
        proxy_pass http://127.0.0.1:8001;
        proxy_read_timeout 300s;
        proxy_buffering off;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
