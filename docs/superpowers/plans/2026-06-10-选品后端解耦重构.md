# 选品比价后端解耦重构 + 错误处理 + 流式 LLM 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 786 行的 `sourcing.py` 按 MVC 分层解耦,4 个外部调用点统一走 httpx + tenacity 做错误分类与指数退避,两处 LLM(SKU 匹配 + 图文核对)改流式并把 token 透传到前端逐字展示,同时把 SKU 匹配 LLM 从 langchain-agent 收回 Python 直连。

**Architecture:** FastAPI 项目按 routers(Controller)/ services(业务编排)/ clients(外部 IO)/ models(Pydantic + 错误体系)/ core(配置日志)分层。错误分类在 client 层判定、重试在 client 层执行,业务层只看到成功或已分类失败。LLM 流式调用把"传输 token"与"解析结构化 JSON"分离:client 内部累积 buffer 解析成结构化结果给业务用,同时回调吐 raw token 供前端逐字看。

**Tech Stack:** Python 3 / FastAPI / httpx(流式 + 连接池) / tenacity(指数退避 + jitter) / Pydantic / pytest + httpx.MockTransport

**设计依据:** `docs/superpowers/specs/2026-06-10-选品后端解耦重构-design.md`

---

## 工作目录

所有路径相对仓库根 `C:\Users\Alan\Desktop\code\software_index_page`。Python 服务根:`backend/python/shopee-analyzer/`(下文简称 `<svc>/`)。

测试运行约定:`cd backend/python/shopee-analyzer && python -m pytest <path> -v`。

## 关键现状参数(实现时对齐,勿臆造)

- 中转 LLM 调用方式(见 `image_verify.py`):`POST {SKU_MATCH_BASE_URL}/chat/completions`,头 `Authorization: Bearer {SKU_MATCH_API_KEY}`,body `{model, messages, temperature, max_tokens}`。
- env:`SKU_MATCH_API_KEY` / `SKU_MATCH_MODEL`(默认 `gpt-5.5`)/ `SKU_MATCH_BASE_URL`(默认 `https://api.openai.com/v1`)。
- SKU 匹配 prompt/schema 现在在 `backend/langchain-agent/src/lib/sku-match.ts`:SYSTEM_PROMPT(108-127 行)+ 输出字段 `matched_sku_id/confidence/reason/scores{price,semantic_match,image_match,supply}/overall_score`。
- SSE 帧格式(见 `sourcing.py:375`):`event: <name>\ndata: <json>\n\n`。
- 限流常量:`_SKU_REQUEST_INTERVAL_SEC`(万邦串行间隔)。
- 前端事件消费在 `src/lib/sourcing.ts` 的 `analyzeStream`(逐帧 generator)。

---

## Step 1 — 基础设施层(core + errors + http)

### Task 1.1: 错误分类体系 models/errors.py

**Files:**
- Create: `<svc>/models/__init__.py`(空文件)
- Create: `<svc>/models/errors.py`
- Test: `<svc>/tests/test_errors.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_errors.py
import httpx
import pytest
from models.errors import (
    SourcingError, RetryableError, PermanentError, UnknownError,
    classify_http_status, classify_exception,
)

@pytest.mark.parametrize("code", [429, 500, 502, 503, 504])
def test_retryable_status(code):
    err = classify_http_status(code, "boom")
    assert isinstance(err, RetryableError)

@pytest.mark.parametrize("code", [400, 401, 403])
def test_permanent_status(code):
    err = classify_http_status(code, "bad")
    assert isinstance(err, PermanentError)

def test_unknown_status_is_permanent_like():
    # 418 未分类 → UnknownError(按不可恢复处理,不重试)
    err = classify_http_status(418, "teapot")
    assert isinstance(err, UnknownError)
    assert not isinstance(err, RetryableError)

def test_timeout_exception_is_retryable():
    err = classify_exception(httpx.ConnectTimeout("t"))
    assert isinstance(err, RetryableError)

def test_connect_error_is_retryable():
    err = classify_exception(httpx.ConnectError("c"))
    assert isinstance(err, RetryableError)

def test_all_subclasses_are_sourcing_error():
    assert issubclass(RetryableError, SourcingError)
    assert issubclass(PermanentError, SourcingError)
    assert issubclass(UnknownError, SourcingError)
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_errors.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'models.errors'`

- [ ] **Step 3: 最小实现**

```python
# models/errors.py
"""选品比价错误分类体系。client 层把原始 HTTP 状态/异常翻译成这三类之一。"""
import httpx

class SourcingError(Exception):
    """所有选品错误的基类。"""
    def __init__(self, message: str, code: str = "", status: int | None = None):
        super().__init__(message)
        self.message = message
        self.code = code
        self.status = status

class RetryableError(SourcingError):
    """可恢复:超时/连接失败/429/5xx/流中断 → tenacity 重试。"""

class PermanentError(SourcingError):
    """不可恢复:400/401/403/余额不足/业务规则 → 立即失败,不重试。"""

class UnknownError(SourcingError):
    """未分类 → 记日志,保守失败(按不可恢复处理,不无限重试)。"""

_RETRYABLE = {429, 500, 502, 503, 504}
_PERMANENT = {400, 401, 403}

def classify_http_status(status: int, message: str) -> SourcingError:
    if status in _RETRYABLE:
        return RetryableError(message, code=f"HTTP_{status}", status=status)
    if status in _PERMANENT:
        return PermanentError(message, code=f"HTTP_{status}", status=status)
    return UnknownError(message, code=f"HTTP_{status}", status=status)

def classify_exception(exc: Exception) -> SourcingError:
    if isinstance(exc, (httpx.TimeoutException, httpx.ConnectError, httpx.ReadError,
                        httpx.RemoteProtocolError)):
        return RetryableError(str(exc), code="NETWORK")
    if isinstance(exc, SourcingError):
        return exc
    return UnknownError(str(exc), code="UNKNOWN")
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest tests/test_errors.py -v`
Expected: PASS(全部)

- [ ] **Step 5: 提交**

```bash
git add backend/python/shopee-analyzer/models/__init__.py backend/python/shopee-analyzer/models/errors.py backend/python/shopee-analyzer/tests/test_errors.py
git commit -m "feat(sourcing): 错误分类体系 RetryableError/PermanentError/UnknownError"
```

### Task 1.2: 共享 httpx + tenacity 重试 clients/http.py

**Files:**
- Create: `<svc>/clients/__init__.py`(空文件)
- Create: `<svc>/clients/http.py`
- Test: `<svc>/tests/test_http_client.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_http_client.py
import httpx
import pytest
from clients.http import request_json, make_retry
from models.errors import RetryableError, PermanentError

def _client_returning(statuses):
    """每次调用按序返回下一个状态码;状态码列表用尽后重复最后一个。"""
    calls = {"n": 0}
    def handler(req):
        i = min(calls["n"], len(statuses) - 1)
        calls["n"] += 1
        code = statuses[i]
        return httpx.Response(code, json={"detail": "x"} if code >= 400 else {"ok": 1})
    return httpx.Client(transport=httpx.MockTransport(handler)), calls

def test_success_returns_json():
    client, calls = _client_returning([200])
    out = request_json(client, "GET", "http://t/x", retry=make_retry(attempts=3, max_wait=0))
    assert out == {"ok": 1}
    assert calls["n"] == 1

def test_503_then_200_retries(monkeypatch):
    client, calls = _client_returning([503, 200])
    out = request_json(client, "GET", "http://t/x", retry=make_retry(attempts=3, max_wait=0))
    assert out == {"ok": 1}
    assert calls["n"] == 2  # 重试了一次

def test_400_not_retried():
    client, calls = _client_returning([400])
    with pytest.raises(PermanentError):
        request_json(client, "GET", "http://t/x", retry=make_retry(attempts=3, max_wait=0))
    assert calls["n"] == 1  # 不重试

def test_503_exhausts_attempts():
    client, calls = _client_returning([503])
    with pytest.raises(RetryableError):
        request_json(client, "GET", "http://t/x", retry=make_retry(attempts=3, max_wait=0))
    assert calls["n"] == 3
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_http_client.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'clients.http'`

- [ ] **Step 3: 最小实现**

```python
# clients/http.py
"""共享 httpx + tenacity 重试。错误分类在此翻译,重试在此执行。"""
import httpx
from tenacity import (Retrying, retry_if_exception_type, stop_after_attempt,
                      wait_random_exponential)
from models.errors import RetryableError, classify_http_status, classify_exception

def make_retry(attempts: int = 3, max_wait: float = 30.0):
    """构造 tenacity Retrying:只对 RetryableError 退避重试。测试时 max_wait=0 免空等。"""
    return Retrying(
        stop=stop_after_attempt(attempts),
        wait=wait_random_exponential(multiplier=1, max=max_wait),
        retry=retry_if_exception_type(RetryableError),
        reraise=True,
    )

def _do_request(client: httpx.Client, method: str, url: str, **kw) -> dict:
    try:
        resp = client.request(method, url, **kw)
    except Exception as e:
        raise classify_exception(e)
    if resp.status_code >= 400:
        raise classify_http_status(resp.status_code, resp.text[:200])
    return resp.json()

def request_json(client: httpx.Client, method: str, url: str, retry=None, **kw) -> dict:
    retry = retry or make_retry()
    return retry(_do_request, client, method, url, **kw)

# 默认共享客户端(连接池);超时细分:连接 5s / 读取按调用方覆盖
def make_client(read_timeout: float = 30.0) -> httpx.Client:
    return httpx.Client(timeout=httpx.Timeout(read_timeout, connect=5.0))
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest tests/test_http_client.py -v`
Expected: PASS(全部)

- [ ] **Step 5: 加依赖 + 提交**

把 `tenacity>=8.0.0` 加入 `<svc>/requirements.txt`(httpx 已在),然后:

```bash
git add backend/python/shopee-analyzer/clients/__init__.py backend/python/shopee-analyzer/clients/http.py backend/python/shopee-analyzer/tests/test_http_client.py backend/python/shopee-analyzer/requirements.txt
git commit -m "feat(sourcing): httpx+tenacity 统一请求层,错误分类与指数退避"
```

---

## Step 2 — 流式 LLM 客户端(clients/llm_client.py)

把 SKU 匹配的 prompt/schema 从 langchain-agent 搬来,与图文核对共用一套流式调用。核心:传输流式 token + 解析结构化 JSON 分离。

### Task 2.1: LLM 结果 Pydantic 模型 models/llm.py

**Files:**
- Create: `<svc>/models/llm.py`
- Test: `<svc>/tests/test_llm_models.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_llm_models.py
import pytest
from pydantic import ValidationError
from models.llm import SkuMatchResult, MatchScores

def test_valid_sku_match():
    r = SkuMatchResult(matched_sku_id="123", confidence=0.8,
                       reason="规格贴合",
                       scores=MatchScores(price=80, semantic_match=90, image_match=50, supply=70),
                       overall_score=85)
    assert r.matched_sku_id == "123"
    assert r.scores.semantic_match == 90

def test_overall_score_out_of_range_rejected():
    with pytest.raises(ValidationError):
        SkuMatchResult(matched_sku_id="1", confidence=0.5, reason="x",
                       scores=MatchScores(price=1, semantic_match=1, image_match=1, supply=1),
                       overall_score=150)

def test_sku_id_accepts_int():
    r = SkuMatchResult(matched_sku_id=999, confidence=0.5, reason="x",
                       scores=MatchScores(price=1, semantic_match=1, image_match=1, supply=1),
                       overall_score=50)
    assert r.matched_sku_id == 999
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_llm_models.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'models.llm'`

- [ ] **Step 3: 最小实现**

```python
# models/llm.py
"""LLM 调用的结构化结果模型(对齐 langchain-agent/sku-match.ts 的 schema)。"""
from pydantic import BaseModel, Field

class MatchScores(BaseModel):
    price: float = Field(ge=0, le=100)
    semantic_match: float = Field(ge=0, le=100)
    image_match: float = Field(ge=0, le=100)
    supply: float = Field(ge=0, le=100)

class SkuMatchResult(BaseModel):
    matched_sku_id: str | int
    confidence: float = Field(ge=0, le=1)
    reason: str
    scores: MatchScores
    overall_score: float = Field(ge=0, le=100)
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest tests/test_llm_models.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/python/shopee-analyzer/models/llm.py backend/python/shopee-analyzer/tests/test_llm_models.py
git commit -m "feat(sourcing): SkuMatchResult Pydantic 模型,对齐 sku-match schema"
```

### Task 2.2: 流式 LLM 调用 stream_chat

**Files:**
- Create: `<svc>/clients/llm_client.py`
- Test: `<svc>/tests/test_llm_client_stream.py`

解析中转 `chat/completions` 的 SSE 流:每行 `data: {json}`,delta 在 `choices[0].delta.content`,结束标记 `data: [DONE]`。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_llm_client_stream.py
import httpx
from clients.llm_client import stream_chat

def _sse_stream(chunks):
    """构造中转风格 SSE 字节流。"""
    body = ""
    for c in chunks:
        body += f'data: {{"choices":[{{"delta":{{"content":"{c}"}}}}]}}\n\n'
    body += "data: [DONE]\n\n"
    return body.encode("utf-8")

def test_stream_accumulates_and_emits_tokens():
    def handler(req):
        return httpx.Response(200, content=_sse_stream(["He", "llo"]),
                              headers={"content-type": "text/event-stream"})
    client = httpx.Client(transport=httpx.MockTransport(handler))
    tokens = []
    full = stream_chat(client, model="gpt-5.5", messages=[{"role":"user","content":"hi"}],
                       api_key="k", base_url="http://t/v1",
                       on_token=tokens.append)
    assert full == "Hello"
    assert tokens == ["He", "llo"]
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_llm_client_stream.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'clients.llm_client'`

- [ ] **Step 3: 最小实现**

```python
# clients/llm_client.py
"""统一中转 LLM 客户端:流式调用,传输 token 与解析结构化结果分离。"""
import json
from collections.abc import Callable
import httpx
from models.errors import classify_http_status, classify_exception

def stream_chat(client: httpx.Client, *, model: str, messages: list,
                api_key: str, base_url: str,
                on_token: Callable[[str], None] | None = None,
                temperature: float = 0, max_tokens: int = 1024) -> str:
    """流式调 chat/completions,累积并返回完整文本;每个 token 经 on_token 吐出。"""
    url = f"{base_url.rstrip('/')}/chat/completions"
    payload = {"model": model, "messages": messages, "temperature": temperature,
               "max_tokens": max_tokens, "stream": True}
    headers = {"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"}
    buf = []
    try:
        with client.stream("POST", url, json=payload, headers=headers) as resp:
            if resp.status_code >= 400:
                resp.read()
                raise classify_http_status(resp.status_code, resp.text[:200])
            for line in resp.iter_lines():
                if not line or not line.startswith("data: "):
                    continue
                data = line[6:]
                if data.strip() == "[DONE]":
                    break
                try:
                    delta = json.loads(data)["choices"][0]["delta"].get("content", "")
                except (json.JSONDecodeError, KeyError, IndexError):
                    continue
                if delta:
                    buf.append(delta)
                    if on_token:
                        on_token(delta)
    except httpx.HTTPError as e:
        raise classify_exception(e)
    return "".join(buf)
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest tests/test_llm_client_stream.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/python/shopee-analyzer/clients/llm_client.py backend/python/shopee-analyzer/tests/test_llm_client_stream.py
git commit -m "feat(sourcing): 流式 LLM 调用 stream_chat,token 累积+回调透传"
```

### Task 2.3: 流式 → 结构化 + 流中断重试 match_sku

**Files:**
- Modify: `<svc>/clients/llm_client.py`(追加 `match_sku` + prompt)
- Test: `<svc>/tests/test_match_sku.py`

把 sku-match.ts 的 SYSTEM_PROMPT 与精简视图逻辑搬到这里。流结束后解析 JSON,失败按 RetryableError 整条重试。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_match_sku.py
import httpx
import pytest
from clients.llm_client import match_sku
from models.errors import RetryableError

_VALID = ('{"matched_sku_id":"7","confidence":0.9,"reason":"贴合",'
          '"scores":{"price":80,"semantic_match":90,"image_match":50,"supply":70},'
          '"overall_score":85}')

def _sse(text):
    return (f'data: {{"choices":[{{"delta":{{"content":{__import__("json").dumps(text)}}}}}]}}\n\n'
            "data: [DONE]\n\n").encode()

def test_match_sku_parses_structured():
    def handler(req):
        return httpx.Response(200, content=_sse(_VALID),
                              headers={"content-type": "text/event-stream"})
    client = httpx.Client(transport=httpx.MockTransport(handler))
    shopee = {"name": "手机壳", "category": "配件", "price_brl": 10.0}
    cand = {"item_id": "7", "title": "硅胶壳", "image_confidence": 0.6,
            "sku": {"items": [{"sku_id": 7, "full_spec": "黑色", "price": "3.5", "can_book_count": 100}]}}
    result, raw = match_sku(client, shopee, cand, api_key="k", base_url="http://t/v1",
                            model="gpt-5.5", max_wait=0)
    assert str(result.matched_sku_id) == "7"
    assert result.overall_score == 85

def test_match_sku_bad_json_retries_then_fails():
    def handler(req):
        return httpx.Response(200, content=_sse("not json at all"),
                              headers={"content-type": "text/event-stream"})
    client = httpx.Client(transport=httpx.MockTransport(handler))
    with pytest.raises(RetryableError):
        match_sku(client, {"name": "x"}, {"item_id": "1", "sku": {"items": [{"sku_id": 1}]}},
                  api_key="k", base_url="http://t/v1", model="m", attempts=2, max_wait=0)
```

- [ ] **Step 2: 运行确认失败**

Run: `python -m pytest tests/test_match_sku.py -v`
Expected: FAIL — `ImportError: cannot import name 'match_sku'`

- [ ] **Step 3: 实现(追加到 clients/llm_client.py)**

```python
# clients/llm_client.py 追加:
import re
from tenacity import Retrying, retry_if_exception_type, stop_after_attempt, wait_random_exponential
from models.errors import RetryableError
from models.llm import SkuMatchResult

# 从 langchain-agent/src/lib/sku-match.ts 搬来(108-127 行原文)
SKU_MATCH_SYSTEM = """你是跨境选品助手。给定一个虾皮(Shopee)在售货品,和【一个】1688 候选货源(含多个分规格 SKU 及其单价/库存,以及一个图文核对得分 image_confidence),请从这个候选的 SKU 里选出与该虾皮货品最匹配的那一个 SKU。

判断依据(按重要性排序):
1. 名称/类目/规格(full_spec)语义贴合度 —— 最重要。
2. image_confidence(图文核对得分,0~1):仅作参考,不能作为唯一依据。该分数普遍偏低且不完全可信。分数高是加分项,分数低不必直接否决;若为 null 表示未核对,按中性对待。
3. 价格竞争力:语义同样贴合时,单价更低更优。
4. 供货能力:库存(can_book_count)更充足更优。

只输出一个 JSON 对象,字段为 matched_sku_id、confidence、reason、scores(含 price/semantic_match/image_match/supply 每项 0-100 整数)、overall_score(0-100)。不要输出 JSON 以外的任何文字。不要计算利润。不要编造 sku_id。"""

def _lean_candidate(candidate: dict) -> dict:
    """裁成 LLM 该看的精简视图(对齐 buildMatchInput)。"""
    items = (candidate.get("sku") or {}).get("items") or []
    return {
        "item_id": candidate.get("item_id", ""),
        "title": candidate.get("title", ""),
        "image_confidence": candidate.get("image_confidence"),
        "skus": [{"sku_id": s.get("sku_id"), "full_spec": str(s.get("full_spec", "")),
                  "price": str(s.get("price", "")), "can_book_count": s.get("can_book_count")}
                 for s in items],
    }

def _parse_sku_match(text: str) -> SkuMatchResult:
    m = re.search(r"\{.*\}", text, re.DOTALL)
    if not m:
        raise RetryableError("LLM 输出无 JSON", code="PARSE")
    try:
        return SkuMatchResult.model_validate_json(m.group(0))
    except Exception as e:
        raise RetryableError(f"LLM JSON 解析失败: {str(e)[:120]}", code="PARSE")

def match_sku(client, shopee: dict, candidate: dict, *, api_key: str, base_url: str,
              model: str, on_token=None, attempts: int = 3, max_wait: float = 20.0):
    """流式调 LLM 选 SKU。返回 (SkuMatchResult, raw_text)。流中断/解析失败 → 整条重试。"""
    lean = {"shopee": {"name": shopee.get("name"), "category": shopee.get("category"),
                       "price_brl": shopee.get("price_brl")},
            "candidate": _lean_candidate(candidate)}
    messages = [{"role": "system", "content": SKU_MATCH_SYSTEM},
                {"role": "user", "content": __import__("json").dumps(lean, ensure_ascii=False)}]

    def _once():
        raw = stream_chat(client, model=model, messages=messages, api_key=api_key,
                          base_url=base_url, on_token=on_token, max_tokens=512)
        return _parse_sku_match(raw), raw

    retry = Retrying(stop=stop_after_attempt(attempts),
                     wait=wait_random_exponential(multiplier=1, max=max_wait),
                     retry=retry_if_exception_type(RetryableError), reraise=True)
    return retry(_once)
```

- [ ] **Step 4: 运行确认通过**

Run: `python -m pytest tests/test_match_sku.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/python/shopee-analyzer/clients/llm_client.py backend/python/shopee-analyzer/tests/test_match_sku.py
git commit -m "feat(sourcing): match_sku 流式选 SKU+结构化解析+流中断整条重试"
```

---

## Step 3 — 各 client 接入 http.py + 错误分类

逐个把 urllib 换成共享 httpx,一次一个文件,每次跑回归。

### Task 3.1: 图文核对接入 llm_client + http

**Files:**
- Modify: `<svc>/image_verify.py`(改用 `clients.llm_client.stream_chat` + httpx)
- Test: `<svc>/tests/test_image_verify.py`(现有,迁移到 MockTransport)

- [ ] **Step 1: 改测试**——把现有 `test_image_verify.py` 里对 urllib 的 mock 改为 `httpx.MockTransport` 喂 SSE 流(参照 test_llm_client_stream.py 的 `_sse_stream`),断言 `verify_one` 仍返回 0~1 confidence、失败返回 None。
- [ ] **Step 2: 运行确认失败**——`python -m pytest tests/test_image_verify.py -v`,Expected: FAIL(还在用 urllib mock)。
- [ ] **Step 3: 实现**——`verify_one` 改为调 `stream_chat`(vision messages 不变,见 image_verify.py:49-57),解析 confidence 复用现有 `_parse_confidence`;异常捕获后仍返回 None(保持"失败不阻塞"语义)。共享 client 由调用方传入。
- [ ] **Step 4: 运行确认通过**——`python -m pytest tests/test_image_verify.py -v`,Expected: PASS。
- [ ] **Step 5: 提交**——`git commit -m "refactor(sourcing): 图文核对改用 httpx 流式 llm_client"`

### Task 3.2: 1688 搜图(aibuy)接入 http + 错误分类

**Files:**
- Modify: `<svc>/aibuy_client.py`(`_mtop_get/post`、`search_by_image`、`_do_fetch_token` 改 httpx)
- Test: `<svc>/tests/test_aibuy_client.py`(新建)

- [ ] **Step 1: 写失败测试**——MockTransport 喂:正常 200 返回商品列表解析正确;503 触发 RetryableError;400 触发 PermanentError 不重试。
- [ ] **Step 2: 运行确认失败**——Expected: FAIL(模块用 urllib)。
- [ ] **Step 3: 实现**——把 `urllib.request.urlopen` 换成 `clients.http.request_json`;token 过期的手动重试(aibuy_client.py:188)保留为业务级重试(401→刷新 token→重发一次),与 tenacity 的网络重试分层不冲突。
- [ ] **Step 4: 运行确认通过**——Expected: PASS。
- [ ] **Step 5: 提交**——`git commit -m "refactor(sourcing): 1688 搜图改 httpx+错误分类,保留 token 刷新"`

### Task 3.3: 万邦 SKU(sku_provider)接入 http + 错误分类

**Files:**
- Modify: `<svc>/sku_provider.py`(`OneboundProvider._request` 改 httpx,attempts=4)
- Test: `<svc>/tests/test_sku_provider.py`(现有,补错误分类用例)

- [ ] **Step 1: 改/补测试**——MockTransport:429 重试(最多 4 次)、200 解析 SKU 列表正确、400 PermanentError。保留现有 `fetch_sku` 失败返回带 error 空结果的语义测试。
- [ ] **Step 2: 运行确认失败**——Expected: FAIL。
- [ ] **Step 3: 实现**——`_request` 用 `request_json` + `make_retry(attempts=4, max_wait=30)`;`fetch_sku` 仍 catch SourcingError 后返回带 error 的空结果(不抛,保持上游不阻塞)。
- [ ] **Step 4: 运行确认通过**——Expected: PASS。
- [ ] **Step 5: 提交**——`git commit -m "refactor(sourcing): 万邦 SKU 改 httpx+429 退避(4 次)"`

---

## Step 4 — services 层(编排 + 纯计算)

### Task 4.1: 成本计算抽成纯函数 services/cost_calculator.py

**Files:**
- Create: `<svc>/services/__init__.py`(空)
- Create: `<svc>/services/cost_calculator.py`
- Test: `<svc>/tests/test_cost_calculator.py`(现有 test_calc_cost_matched_sku 迁移)

- [ ] **Step 1: 迁移测试**——把现有成本/利润/推荐等级测试指向新模块。
- [ ] **Step 2: 运行确认失败**——Expected: FAIL(新模块不存在)。
- [ ] **Step 3: 实现**——从 sourcing.py 抽出 `_build_row` 内的成本/利润/margin_rate/recommendation 纯计算逻辑到 `cost_calculator.py`,无 IO,输入 SKU 单价 + cost_cfg,输出成本字段。
- [ ] **Step 4: 运行确认通过**——Expected: PASS。
- [ ] **Step 5: 提交**——`git commit -m "refactor(sourcing): 成本计算抽成纯函数 services/cost_calculator"`

### Task 4.2: 编排逻辑抽到 services/sourcing_pipeline.py

**Files:**
- Create: `<svc>/services/sourcing_pipeline.py`
- Test: `<svc>/tests/test_sourcing_pipeline.py`(新建)

- [ ] **Step 1: 写失败测试**——注入 fake clients(搜图返回固定候选、sku_provider 返回固定 SKU、llm match_sku 返回固定 result),断言:① 编排产出阶段事件序列(search→sku→image_verify→match);② 单个候选 match_sku 抛 PermanentError 时该候选标 llm_failed 但其他候选继续(不阻塞批次)。
- [ ] **Step 2: 运行确认失败**——Expected: FAIL。
- [ ] **Step 3: 实现**——把 sourcing.py 的 `_fetch_skus_into`/`_match_all`/`_build_rows` 编排搬到 pipeline,clients 由参数注入(依赖倒置);match_sku 改调 `llm_client.match_sku`;单候选失败 catch 后标记不中断(对齐 sku_match_client 原 "失败不阻塞" 语义)。limits/间隔保留:`time.sleep(_SKU_REQUEST_INTERVAL_SEC)` 作主动节流,503 退避交 tenacity。
- [ ] **Step 4: 运行确认通过**——Expected: PASS。
- [ ] **Step 5: 提交**——`git commit -m "refactor(sourcing): 编排逻辑抽到 services/sourcing_pipeline,clients 注入"`

---

## Step 5 — routers 瘦身 + SSE 新增 llm_token 事件

### Task 5.1: 选品路由瘦身 routers/sourcing.py

**Files:**
- Create: `<svc>/routers/__init__.py`(空)
- Create: `<svc>/routers/sourcing.py`(从 sourcing.py 抽路由)
- Modify: `<svc>/main.py`(挂载新 router)
- Test: `<svc>/tests/test_api.py`(现有,确认端点不变)

- [ ] **Step 1: 确认现有 API 测试覆盖端点**——若缺,补 TestClient 对 /api/sourcing/config、/sku-provider/status 的用例。
- [ ] **Step 2: 运行(基线绿)**——`python -m pytest tests/test_api.py -v`,记录当前通过。
- [ ] **Step 3: 实现**——把 sourcing.py 的 FastAPI 路由(search/analyze/analyze-stream/config/system/reload/sku-provider/status)搬到 routers/sourcing.py,路由内只做入参校验/文件读取/调 pipeline/拼 SSE。SSE 编排里把 `match_sku` 的 `on_token` 回调接到新事件:`_sse("llm_token", {"item_id": iid, "phase": "sku_match", "token": tok})`;候选评分完发 `_sse("candidate_scored", {...})`;流中断重试发 `_sse("llm_retry", {"item_id": iid})`。心跳逻辑保留作兜底。
- [ ] **Step 4: 运行确认通过**——`python -m pytest tests/test_api.py -v`,Expected: PASS(端点行为不变 + 新事件)。
- [ ] **Step 5: 提交**——`git commit -m "refactor(sourcing): 路由瘦身到 routers/,SSE 加 llm_token/candidate_scored/llm_retry"`

### Task 5.2: 店铺分析路由拆出 routers/shopee_store.py

**Files:**
- Create: `<svc>/routers/shopee_store.py`
- Modify: `<svc>/main.py`
- Test: `<svc>/tests/test_api.py`

- [ ] **Step 1: 补/确认测试**——TestClient 覆盖 /api/analyze、/api/diagnose、/api/health。
- [ ] **Step 2: 运行(基线)**——记录通过。
- [ ] **Step 3: 实现**——把 main.py 里店铺分析端点(analyze/diagnose/simulate/rules/search-image/health)搬到 routers/shopee_store.py;堆栈不再返回客户端(`traceback.format_exc()` 仅进日志,返回 `{code, message}`)。main.py 只剩 app 装配 + include_router。
- [ ] **Step 4: 运行确认通过**——Expected: PASS。
- [ ] **Step 5: 提交**——`git commit -m "refactor(sourcing): 店铺分析路由拆出,堆栈不再泄露给客户端"`

---

## Step 6 — 前端逐字展示

### Task 6.1: sourcing.ts 处理 llm_token 事件

**Files:**
- Modify: `src/lib/sourcing.ts`(新增事件类型)
- Modify: `src/components/sourcing/ResultTable.tsx`(逐字展示 UI)
- Test: 前端 `pnpm tsc -b` 类型检查 + 浏览器手测

- [ ] **Step 1: 类型与解析**——`SseEvent` 已是通用结构,无需改 generator;在消费侧(SourcingToolPage / ResultTable)新增对 `event === "llm_token"`(按 item_id 累积 token 到该候选的实时文本)、`candidate_scored`(定格 match_overall_score)、`llm_retry`(清空该候选已显示 token)的处理。
- [ ] **Step 2: UI**——候选行/明细在 sku_match 阶段显示流式累积文本(打字机),candidate_scored 后切换为最终评分。用 lucide 图标,Tailwind 设计令牌,禁硬编码色。
- [ ] **Step 3: 类型检查**——`pnpm tsc -b`,Expected: 无错。
- [ ] **Step 4: 浏览器手测**——启动 `pnpm dev` + Python 服务,跑一次选品,确认逐字出现、最终定格、单候选失败不卡整批。
- [ ] **Step 5: 提交**——`git commit -m "feat(sourcing): 前端逐字展示 SKU 匹配 LLM 输出"`

---

## Step 7 — 删旧链路(新链路验证稳定后)

> 前 6 步全绿且浏览器验证稳定后才执行。删前确认 langchain-agent sku-match 无其他调用方。

### Task 7.1: 删除跨服务 SKU 匹配旧链路

**Files:**
- Delete: `<svc>/sku_match_client.py`(+ tests/test_sku_match_client.py)
- Delete: `backend/langchain-agent/src/routes/sku-match.ts`、`src/lib/sku-match.ts`、`tests/sku-match.test.ts`
- Modify: `backend/langchain-agent/src/index.ts`(移除 skuMatchRouter 挂载)

- [ ] **Step 1: 确认无引用**——`grep -rn "sku-match\|sku_match_client\|skuMatchRouter" backend src` 仅剩待删文件自身。
- [ ] **Step 2: 删除 + 卸载路由**——删文件,从 index.ts 移除 import 与 `app.use(.../sku-match...)`。
- [ ] **Step 3: 类型检查**——`cd backend/langchain-agent && npx tsc --noEmit` 无错;Python `python -m pytest` 全绿。
- [ ] **Step 4: 回归**——浏览器再跑一次选品确认 SKU 匹配正常(走 Python 直连)。
- [ ] **Step 5: 提交**——`git commit -m "refactor(sourcing): 删除 langchain-agent sku-match 旧链路,SKU 匹配全由 Python 直连"`

---

## 最终验证(部署前,对齐 CLAUDE.md 部署规范)

- [ ] `cd backend/python/shopee-analyzer && python -m pytest -v` 全绿
- [ ] `pnpm tsc -b`(前端)无错
- [ ] `cd backend/langchain-agent && npx tsc --noEmit` 无错
- [ ] `git status` 确认无遗漏未追踪源码文件(新增的 models/clients/services/routers 全部 git add)
- [ ] requirements.txt 含 tenacity;httpx 实际启用
