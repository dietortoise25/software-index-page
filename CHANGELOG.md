# Changelog

## v1.3.0 (2026-05-14)

### 新增
- 退货工作流 Web 工具：上传 TikTok/Shopee 退单 Excel，自动分类录入飞书多维表格
- 配置面板：Web 端可视化管理飞书连接、表格映射、运行参数，带人话解释
- 使用指引卡片：数据源下载路径、多维表格查看向导
- 处理进度详情：去重计数、待入库数、下载+上传进度
- 任务完成后展示飞书多维表格直达链接
- 图片全自动流水线：CDN 下载 → 飞书 Drive 上传 → 记录附件绑定
- 自动 wiki token → base token 解析
- 部署脚本 `scripts/deploy-full.sh`（服务器端构建，一键全量部署）
- E2E 测试 `tests/e2e-return-workflow.mjs`（33 断言覆盖）

### 修复
- 字段错位：FIELD_NAMES 遗漏「图片」字段导致全部数据错列
- 数据丢失：merger 未调用 normalizeRow 导致 metaFields 全为 undefined
- 图片下载：validateStatus 拒绝 301/302 重定向
- 图片上传：wiki token 作为 Drive parent_node 被拒绝 → 自动解析
- 图片上传：parent_type 从 bitable_file 改为 bitable_image
- 限流：10 并发 → 串行 1.2s 间隔 + 25s 超时兜底
- 卡死防护：Promise.race + AbortController 双重超时

### 变更
- 部署 Skill 升级 v2.0.0（覆盖新架构 + 退货工作流 API）
- .gitignore 新增 tools/*/dist/、output/、.tmp_* 规则
- 图片默认并发数 5 → 10（后续改为串行）

## v1.2.0 (2026-05-12)

### 新增
- 审查面板审批确认对话框（通过需二次确认，驳回用内联 textarea 替代浏览器 prompt）
- 审批通过后自动在飞书日历「消息转发助手」中创建开发排期日程
- 飞书通知闭环：新需求提交、审批通过、驳回时均发送飞书消息通知
- 审查面板搜索功能（按标题/提交人过滤）
- 已处理需求显示审查时间
- 退出管理者模式按钮（替代锁定）
- Playwright E2E 自动化测试脚本（27 项覆盖双角色旅程）

### 修复
- PIN 强制检查：未设置 REVIEW_PIN 环境变量时服务拒绝启动
- PIN 暴力破解保护：60 秒内最多 5 次错误尝试
- PIN 不再通过 URL 查询参数传递（改用 POST body）
- 审批操作原子性保证（先创建日历事件和发卡片，最后更新状态）
- JSON 文件写入互斥锁，防止并发覆盖
- 移除飞书密钥硬编码回退值，强制要求环境变量
- AI 聊天提交成功改为手动关闭（不再 3 秒自动消失）
- 排期加载 30 秒超时保护
- useChat error 状态处理（错误气泡 + 重试）
- AI 未输出 JSON 时显示提示引导
- 审查面板需求列表按提交时间降序排列
- React hooks 顺序修复（useMemo 在条件返回前）

### 变更
- 审查面板按钮文字「锁定」→「退出管理者模式」
- 飞书日历 ID 更新为机器人主日历，Alan 已被添加为 writer 成员
- `FEISHU_APP_SECRET` 从可选变为必需环境变量

---

## v1.1.0 (2026-05-12)

### 新增
- AI 需求助手可读取飞书日历空闲时段，智能生成开发排期
- 新增 `/review` 需求审查面板，PIN 码保护，管理员可审批/驳回/重新排期
- 审批通过后自动在飞书日历创建日程事件
- 新增 `/changelog` 更新日志页面

### 修复
- 修复 Header 在手机端不可见的问题，新增汉堡菜单
- AI 对话需求提交改为存入审查面板，Alan 审批后才发送飞书卡片
- 快速表单提交的需求同步写入审查面板
- 两个提交通道（AI对话 + 快速表单）现在统一汇入审查面板

### 变更
- 飞书需求卡片从纯文本升级为互动卡片格式
- 浮动按钮添加文字标签
- Footer 显示版本号

---

## v1.0.0 (2026-05-10)

### 新增
- 软件工具发布站上线，支持工具库浏览和下载
- AI 需求助手：4 阶段引导式需求收集
- 快速需求提交表单
- 文章系统：支持 Markdown 文章发布
- Ocean Breeze 主题，支持暗色模式
- 首页动态 Hero 区域
